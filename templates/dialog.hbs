<div class="ai-scene-generator-wrapper">

  {{!-- Prompt Section --}}
  <div class="section prompt-section">
    <label class="section-label">
      <i class="fas fa-pen-nib"></i>
      {{localize "AISCENEGEN.PromptLabel"}}
    </label>
    <textarea
      class="prompt-input"
      rows="4"
      placeholder="{{localize "AISCENEGEN.PromptPlaceholder"}}"
    ></textarea>
  </div>

  {{!-- Style Picker --}}
  <div class="section style-section">
    <label class="section-label">
      <i class="fas fa-paint-brush"></i>
      {{localize "AISCENEGEN.StyleLabel"}}
    </label>
    <div class="style-grid">
      {{#each styles}}
      <button
        class="style-btn {{#if active}}active{{/if}}"
        data-style="{{id}}"
        title="{{label}}"
        type="button"
      >
        <i class="fas {{icon}}"></i>
        <span>{{label}}</span>
      </button>
      {{/each}}
    </div>
  </div>

  {{!-- Scene Name --}}
  <div class="section scene-name-section">
    <label class="section-label">
      <i class="fas fa-map"></i>
      {{localize "AISCENEGEN.SceneNameLabel"}}
    </label>
    <input
      type="text"
      class="scene-name-input"
      placeholder="{{localize "AISCENEGEN.SceneNamePlaceholder"}}"
    />
  </div>

  {{!-- Actions --}}
  <div class="section actions-section">
    <button class="btn-generate" type="button">
      <i class="fas fa-wand-magic-sparkles"></i>
      <span>{{localize "AISCENEGEN.Generate"}}</span>
    </button>
    <div class="generate-status"></div>
    <div class="spinner" style="display:none;">
      <i class="fas fa-spinner fa-spin"></i>
    </div>
  </div>

  {{!-- Preview --}}
  <div class="section preview-section" style="display:none;">
    <label class="section-label">
      <i class="fas fa-eye"></i>
      {{localize "AISCENEGEN.PreviewLabel"}}
    </label>
    <div class="preview-container">
      <img class="preview-img" src="" alt="Generated scene preview" />
    </div>
    <details class="prompt-details">
      <summary>{{localize "AISCENEGEN.FullPromptLabel"}}</summary>
      <p class="full-prompt-display"></p>
    </details>
    <div class="preview-actions">
      <button class="btn-regenerate" type="button" disabled>
        <i class="fas fa-rotate"></i>
        {{localize "AISCENEGEN.Regenerate"}}
      </button>
      <button class="btn-create-scene" type="button" disabled>
        <i class="fas fa-plus"></i>
        {{localize "AISCENEGEN.CreateScene"}}
      </button>
    </div>
  </div>

</div>
